import { LessonRegistration } from './lesson-registration';

describe('LessonRegistration', () => {
  it('should create an instance', () => {
    expect(new LessonRegistration()).toBeTruthy();
  });
});

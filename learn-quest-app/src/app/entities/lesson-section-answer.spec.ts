import { LessonSectionAnswer } from './lesson-section-answer';

describe('LessonSectionAnswer', () => {
  it('should create an instance', () => {
    expect(new LessonSectionAnswer()).toBeTruthy();
  });
});
